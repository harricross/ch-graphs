[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = [
    "E501",   # line too long — handled by line-length above, long strings are acceptable
]

[tool.pytest.ini_options]
testpaths = ["tests"]
